name: Publish Eldoria Preview

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Build consolidated r7
        run: node tools/build-r7.mjs
      - name: Structural publication checks
        run: |
          test -s playtest/index.html
          ! grep -q '../v0195-r6/index.html' playtest/index.html
          grep -q '0.19.5-r7' playtest/index.html
          grep -q 'onboardingDone' playtest/index.html
          grep -q 'MAR DE CRISTAL' playtest/index.html
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
