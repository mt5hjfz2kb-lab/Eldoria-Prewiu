# Eldoria — Web Playtest

Publicación web del vertical slice de Eldoria.

## Candidata actual

**v0.19.5 r7 — consolidated playtest**

La publicación genera una única build jugable en `playtest/` mediante `tools/build-r7.mjs`. El navegador ya no ejecuta la cadena histórica r4 → r5 → r6 → r7 para esta ruta de prueba.

La raíz de GitHub Pages redirige a `playtest/`.

## Historial conservado

Las rutas anteriores (`v0192`, `v0193`, `v0194`, `v0195`, `v0195-clean`, `v0195-r4`, `v0195-r5`, `v0195-r6`, `v0195-r7`, `v0195-stable`) se mantienen como histórico y recuperación.

## Regla

No considerar r7 estable hasta completar un playtest móvil de principio a fin. Los criterios están en `docs/QA_R7.md` y la baseline de consolidación en `docs/R7_CONSOLIDATION.md`.
